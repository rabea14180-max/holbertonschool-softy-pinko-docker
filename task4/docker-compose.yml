version: "3"

services:
  back-end:
    build:
      context: ./
      dockerfile: Dockerfile
    ports:
      - "5252:5252"

  front-end:
    build:
      context: ./
      dockerfile: front-end.Dockerfile
    ports:
      - "9000:9000"
